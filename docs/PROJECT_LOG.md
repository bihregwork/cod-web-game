# Project Log

## 2026-07-20

### Stage 0. Discovery / Опрос и сбор информации

- Статус: In Progress.
- Старт этапа выполнен на основе `docs/PROJECT_PLAN.md`.
- Создан документ `docs/DISCOVERY.md` для фиксации вопросов, ответов и вводных.
- Создан документ `docs/DECISIONS.md` для фиксации принятых решений.
- Зафиксировано правило Stage Gate: следующий этап начинается только после принятия результата текущего этапа заказчиком.
- Зафиксировано принятое решение по таблице рекордов: использовать анонимный локальный `playerId`.
- Получены и записаны первые ответы заказчика по целям, аудитории, сценарию, визуалу, управлению, рекордам и критериям успеха.
- Создана папка для визуальных референсов: `docs/references/visual`.
- Отмечены вопросы, требующие уточнения: изменение отображаемого имени и ограничения v1.
- Заказчик принял комментарии Codex по пунктам "Уточнить"; рекомендации записаны как решения.
- Скорректирована длительность игровой попытки: игра длится столько, на сколько хватит жизней, при растущей скорости и частоте падения предметов.

### Next

- Сформировать финальный итог Discovery.
- Запросить принятие результата Discovery и разрешение перейти к Requirements.

## 2026-07-21

### Stage 0. Discovery / Опрос и сбор информации

- Статус: Accepted.
- Заказчик подтвердил движение дальше фразой "Двигаемся далее".
- В `docs/DISCOVERY.md` зафиксирован финальный итог Discovery и условие перехода к Requirements.

### Stage 1. Requirements / Требования

- Статус: In Progress.
- Создан документ `docs/REQUIREMENTS.md`.
- На основе Discovery подготовлен черновик требований v1.
- Выделены открытые вопросы Requirements по жизням, очкам, вредным предметам, росту сложности и машинной бонусной механике.
- Зафиксировано стартовое количество жизней: 3.
- Зафиксирован старт игры: кнопка "Старт" на основном игровом экране, без отдельного стартового экрана.
- Зафиксировано, что запчасти машины собирают бонус и не дают очки напрямую.
- Зафиксирована первичная модель роста сложности; актуальная runtime-модель позднее уточнена и теперь растет только по очкам.
- Созданы отдельные документы для проработки: `docs/SCORING_AND_HAZARDS.md` и `docs/MACHINE_BONUS_SCENARIO.md`.
- По указанию заказчика изменена логика работы: предложения Codex по открытым вопросам сначала выводятся в чат и записываются в документы только после согласования.
- Неподтвержденные предложения по очкам, вредным предметам, машинному бонусу и параметрам роста сложности удалены из рабочих документов до согласования.
- Заказчик утвердил правила очков и вредных предметов: купюры 500/1000/5000, мебель, договор, налог, штраф и пропущенный договор.
- Заказчик утвердил исключение купюр 100 и 200 из v1.
- Заказчик утвердил бензин как канистру 20 литров: +20 литров, 1 литр = 1 секунда движения на машине.
- Обновлены `docs/SCORING_AND_HAZARDS.md`, `docs/MACHINE_BONUS_SCENARIO.md`, `docs/REQUIREMENTS.md` и `docs/DECISIONS.md`.
- Заказчик уточнил визуал: на основном экране героиня в деловом костюме с большим кошельком, куда попадают предметы.
- Заказчик уточнил визуал машинного бонуса: желтый седан 2002 года в стиле Mazda Atenza, без логотипов; из седана видна героиня с большим кошельком.
- Зафиксировано, что фото героини будет предоставлено позже как визуальный референс.
- Заказчик утвердил первичные параметры смешанного роста сложности; актуальная runtime-модель позднее уточнена: рост по времени отключен, рост по очкам `+1 уровень за каждые 200000 очков`.
- Зафиксирована первичная доля вредных предметов: от 6% до 15%, внутри вредных налог 65%, штраф 35%; актуальная runtime-пропорция позднее уточнена до `70% / 30%`.
- Зафиксирована доля договора: от 2% до 5%, с защитой от слишком частого и слишком редкого выпадения.
- Создан документ `docs/DIFFICULTY_BALANCE.md`.
- Заказчик утвердил оставшиеся правила машинного бонуса: сборка в любом порядке, дубли без очков и без поломки сборки, активация при полной сборке и наличии бензина.
- Заказчик утвердил максимум бензина 60 литров и частоту выпадения канистр по состоянию машины и запасу бензина.
- Зафиксировано, что машина не защищает от налога, штрафа и пропущенного договора.
- Обновлены `docs/MACHINE_BONUS_SCENARIO.md`, `docs/REQUIREMENTS.md` и `docs/DECISIONS.md`.
- Заказчик утвердил вероятности выпадения предметов: купюры, мебель, запчасти, бензин, договор и вредные предметы.
- Создан документ `docs/ITEM_SPAWN_PROBABILITIES.md`.
- В `docs/REQUIREMENTS.md` отмечено, что открытых вопросов Requirements больше нет.

### Next

- Запросить принятие Requirements и разрешение перейти к Design.

## 2026-07-21

### Stage 1. Requirements / Требования

- Статус: Accepted.
- Заказчик принял этап Requirements и разрешил перейти к Design.

### Stage 2. Design / Проектирование

- Статус: In Progress.
- Созданы стартовые Design-документы:
  - `docs/DESIGN.md`;
  - `docs/SCREEN_FLOW.md`;
  - `docs/GAME_LOOP_DESIGN.md`;
  - `docs/VISUAL_BRIEF.md`;
  - `docs/ASSET_PROMPTS.md`;
  - `docs/API_CONTRACT.md`.
- Зафиксировано правило Design: предложения и prompts сначала выводятся в чат, а в документы записываются только после согласования.
- Заказчик добавил фоновые референсы `tomsk-collage-desktop.png` и `tomsk-collage-mobile.png`.
- Заказчик принял структуру основного экрана и HUD.
- Зафиксирован desktop layout: счет сверху слева, жизни/пауза/рестарт/рекорды строкой ниже, HUD машины сверху справа.
- Зафиксирован mobile layout: очки и жизни в верхней строке, машина и бензин во второй строке, управляющие кнопки иконками справа.
- Зафиксированы состояния HUD машины: пустой слот, собранная деталь, `Нужен бензин`, активная подсветка и убывающий бензин.
- Заказчик принял модель игрового цикла и hitbox-логики.
- Зафиксировано, что игровой цикл работает через `requestAnimationFrame`.
- Зафиксированы состояния `idle`, `playing`, `paused`, `carMode`, `gameOver`, `leaderboard`.
- Зафиксировано, что ловля предметов считается по hitbox большого кошелька, а не всей героини.
- Зафиксированы правила пропусков, паузы, Game Over, Car Mode и первичная модель неуязвимости после потери жизни; актуальная runtime-защита уточнена позже до `700 мс`.
- Обновлены `docs/GAME_LOOP_DESIGN.md`, `docs/SCREEN_FLOW.md` и `docs/DECISIONS.md`.
- Заказчик принял визуальный бриф: яркая рисованная 2D arcade, средняя карикатурность героини, фото как референс, игрушечные предметы, разные стили мебели и Tomsk collage как фон.
- Обновлены `docs/VISUAL_BRIEF.md` и `docs/DECISIONS.md`.
- Заказчик согласовал prompt pack для ассетов.
- Prompts записаны в `docs/ASSET_PROMPTS.md`.
- Обновлен `docs/DECISIONS.md`.

### Next

- Сгенерировать 2-3 визуальных направления для героини и машины.
- После выбора визуального направления перейти к API-контракту.

### Stage 2. Design / Проектирование - visual baseline

- Статус: In Progress.
- Заказчик выбрал направление `retro arcade / sticker style`.
- Утверждены два визуальных концепта: героиня без авто и героиня в car mode.
- Концепт героини без авто сохранен в `assets/concepts/heroine-concept.png`.
- Концепт героини в машине сохранен в `assets/concepts/car-mode-concept.png`.
- Зафиксированы ключевые признаки: крупный кошелек как зона ловли, деловой костюм, комичная дружелюбная подача, желтый седан 2002 года в стиле Mazda Atenza без логотипов.

### Next

- Перейти к следующему Design-блоку: состав игровых экранов и интерфейсные состояния на основе утвержденного визуального направления.

### Stage 2. Design / Проектирование - уточнение visual baseline

- Статус: In Progress.
- Заказчик принял обновленную пару изображений героини без авто и героини в car mode.
- Файлы `assets/concepts/heroine-concept.png` и `assets/concepts/car-mode-concept.png` заменены на новую согласованную пару.
- Зафиксировано требование визуального совпадения: прическа, элементы одежды, украшения, пропорции героини и размер кошелька должны быть одинаковыми в двух режимах.

### Next

- Перейти к следующему Design-блоку: состав игровых экранов и интерфейсные состояния на основе утвержденного visual baseline.

### Stage 2. Design / Проектирование - игровые экраны и состояния

- Статус: In Progress.
- Заказчик принял модель игровых экранов и интерфейсных состояний.
- Зафиксированы состояния `Idle`, `Countdown`, `Playing`, `Car Mode`, `Paused`, `Leaderboard`, `Game Over`.
- Зафиксировано, что основной экран остается единым, а пауза, рекорды и Game Over открываются поверх игрового поля.
- Зафиксировано, что `Car Mode` является визуальным состоянием игрового поля, а не отдельным экраном.
- Обновлены `docs/SCREEN_FLOW.md`, `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Перейти к следующему Design-блоку: полный набор ассетов для v1 и правила их подготовки.

### Stage 2. Design / Проектирование - полный набор ассетов

- Статус: In Progress.
- Заказчик принял полный набор ассетов v1 и правила их подготовки.
- Создан документ `docs/ASSET_LIST.md`.
- Зафиксированы группы ассетов: персонаж, car mode, фоны, купюры, договор, мебель, вредные предметы, детали машины, бензин, HUD, UI, эффекты и стандартные звуки.
- Зафиксированы правила: единый `retro arcade / sticker style`, прозрачный фон для игровых объектов, читаемость на desktop/mobile, отсутствие логотипов, текст `НАЛОГ` и `ШТРАФ` накладывается кодом.
- Обновлены `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Перейти к следующему Design-блоку: API-контракт таблицы рекордов или структура проекта.

### Stage 2. Design / Проектирование - API-контракт таблицы рекордов

- Статус: In Progress.
- Заказчик принял API-контракт таблицы рекордов.
- Зафиксированы endpoints `GET /.netlify/functions/scores` и `POST /.netlify/functions/scores`.
- Зафиксирована связка Netlify Functions + Netlify Blobs для v1.
- Зафиксированы правила `playerId`, top-10, обновления личного лучшего результата, смены отображаемого имени и frontend fallback через `localStorage`.
- Обновлены `docs/API_CONTRACT.md`, `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Stage 2. Design / Проектирование - порядок asset production preview

- Статус: In Progress.
- Заказчик принял, что после API-контракта и структуры проекта будет Design-блок `asset production preview`.
- Ассеты будут генерироваться партиями до Environment Setup и переноситься в финальные папки только после preview и явного принятия.

### Next

- Перейти к следующему Design-блоку: структура проекта.

### Stage 2. Design / Проектирование - структура проекта

- Статус: In Progress.
- Заказчик принял структуру проекта.
- Создан документ `docs/PROJECT_STRUCTURE.md`.
- Зафиксировано разделение на `docs`, `assets`, `public/assets`, `src`, `netlify/functions` и `tests`.
- Зафиксировано, что игровая логика будет в `src/game`, React UI - в `src/ui`, API и `localStorage`-сервисы - в `src/services`.
- Зафиксировано, что backend таблицы рекордов будет в `netlify/functions/scores.ts`.
- Обновлены `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Перейти к Design-блоку `asset production preview` и сгенерировать предметы партиями до Setup.

### Stage 2. Design / Проектирование - asset production preview, партия 1

- Статус: In Progress.
- Заказчик принял первую preview-партию полезных предметов.
- Приняты купюры 500/1000/5000 и договор.
- Зеркало принято после замены на более современный вид.
- Кухня принята после замены бирюзового цвета на металлический оттенок.
- Прихожая принята после добавления теплых деревянных оттенков.
- Принятые preview-файлы сохранены в `assets/items`.
- Зафиксировано, что техническая подготовка прозрачного фона/обрезки выполняется отдельным шагом перед подключением ассетов в игру.
- Обновлены `docs/ASSET_LIST.md`, `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Перейти к следующей preview-партии: вредные предметы `tax` и `fine`.

### Stage 2. Design / Проектирование - asset production preview, партия 2

- Статус: In Progress.
- Заказчик принял preview-партию вредных предметов.
- Принят `tax.png`: прямоугольный бюрократический документ с крупными буквами `ФНС`.
- Принят `fine.png`: прямоугольный светлый документ с крупными буквами `ДПС` и красной линией по периметру.
- Принятые preview-файлы сохранены в `assets/items`.
- Обновлены `docs/ASSET_LIST.md`, `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Перейти к следующей preview-партии: машинный бонус, то есть колесо, двигатель, корпус и канистра бензина 20 л.

### Stage 2. Design / Проектирование - asset production preview, партия 3

- Статус: In Progress.
- Заказчик принял preview-партию машинного бонуса.
- Приняты `wheel.png`, `engine.png`, `car-body.png` и `fuel-can-20l.png`.
- Принятые preview-файлы сохранены в `assets/items`.
- Обновлены `docs/ASSET_LIST.md`, `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Перейти к следующему Design-блоку: HUD/effects или техническая подготовка ассетов перед Setup.

### Stage 2. Design / Проектирование - уточнение лимита бензина

- Статус: In Progress.
- Заказчик изменил максимум бензина с 120 л на 60 л.
- 60 л бензина дают 60 секунд запаса движения при полном баке.
- 3 канистры по 20 л дают максимальный запас 60 л.
- При 60 л и несобранной машине новые канистры не падают, шанс канистры 0%.
- Обновлена первичная шкала выпадения бензина: машина не собрана - 2%; машина не собрана, бензина 60 л - 0%; машина собрана, бензина 0 - 4%; машина активна, бензина 1-20 л - 3%; машина активна, бензина 21-40 л - 2%; машина активна, бензина 41-60 л - 1%; актуальная runtime-шкала позднее уточнена.
- Обновлены `docs/REQUIREMENTS.md`, `docs/MACHINE_BONUS_SCENARIO.md`, `docs/ITEM_SPAWN_PROBABILITIES.md`, `docs/GAME_LOOP_DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Продолжить Design-блок: HUD/effects или техническая подготовка ассетов перед Setup.

### Stage 2. Design / Проектирование - desktop screen mockup

- Статус: Accepted.
- Заказчик принял основной desktop screen mockup игрового экрана.
- Утвержденный preview: `assets/technical-preview/screen-mockup-desktop-v27-hearts-more-left.png`.
- Текущий основной preview обновлен: `assets/technical-preview/screen-mockup-desktop.png`.
- Зафиксирована компактная HUD-компоновка: левое и правое меню по 390 px шириной и 104 px высотой.
- Левое меню содержит очки, жизни и кнопки `Пауза`, `Рестарт`, `Рекорды`.
- Правое меню содержит сборку машины, статус `Собери машину`, бензин `40/60 л` и слоты деталей.
- Обновлены `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Следующий рекомендуемый Design-preview: `Car Mode` mockup desktop, чтобы проверить героиню в машине с тем же HUD.

### Stage 2. Design / Проектирование - desktop Car Mode screen mockup

- Статус: Accepted.
- Заказчик принял desktop mockup режима `Car Mode`.
- Утвержденный финальный preview: `assets/technical-preview/screen-mockup-car-mode-desktop-v7-scale-matched-wallet.png`.
- Масштаб машины с героиней уменьшен так, чтобы ширина кошелька соответствовала основному desktop-экрану.
- Текущий preview режима `Car Mode` обновлен: `assets/technical-preview/screen-mockup-car-mode-desktop.png`.
- В `Car Mode` героиня отображается в желтом седане, кошелек остается видимой зоной ловли.
- Правый HUD показывает активный режим, собранные детали и бензин `38/60 л`.
- В preview активного режима запчасти не падают; падают обычные, вредные предметы и бензин.
- Обновлены `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Следующий рекомендуемый Design-preview: mobile screen mockup основного игрового экрана.

### Stage 2. Design / Проектирование - уточнение продолжительности Car Mode

- Статус: In Progress.
- Заказчик уточнил правило продолжительности `Car Mode`.
- `Car Mode` длится, пока не закончится бензин.
- Запас бензина не может быть больше 60 л в любой момент.
- 60 л означают максимальный запас времени в баке, а не жесткий лимит одной активации.
- Пойманная во время `Car Mode` канистра может продлить текущий режим, но запас бензина остается ограничен 60 л.
- Обновлены `docs/REQUIREMENTS.md`, `docs/MACHINE_BONUS_SCENARIO.md`, `docs/GAME_LOOP_DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Продолжить screen mockup preview или перейти к следующему Design-блоку после согласования текущего экрана.

### Stage 2. Design / Проектирование - HUD/effects preview

- Статус: In Progress.
- Заказчик принял модель HUD/effects.
- Создан документ `docs/HUD_EFFECTS.md`.
- Зафиксирован HUD: счет, жизни, кнопки, HUD машины, бензин `0/60 л`, статусы `Собери машину`, `Нужен бензин`, `Car Mode`.
- Зафиксированы эффекты: pop-up очков, вспышки, красный flash, pulse слотов, плашка `Машина собрана!`, старт `Car Mode`, окончание бензина.
- Зафиксировано, что отдельные картинки для HUD/effects в v1 не генерируются; эффекты, слоты, шкала, кнопки и плашки реализуются кодом/CSS/canvas.
- Обновлены `docs/DESIGN.md`, `docs/ASSET_LIST.md` и `docs/DECISIONS.md`.

### Next

- Перейти к технической подготовке ассетов перед Setup или запросить принятие Design-этапа.

### Stage 2. Design / Проектирование - техническая подготовка ассетов

- Статус: In Progress.
- Исходные утвержденные preview сохранены в `assets/source-previews`.
- Добавлен скрипт `scripts/prepare_assets.py`.
- Игровые предметы подготовлены как RGBA PNG с прозрачным фоном, обрезкой и максимальным размером 512 px по длинной стороне.
- Персонажи подготовлены как RGBA PNG с прозрачным фоном и максимальным размером 1024 px по длинной стороне.
- Подготовленные персонажи сохранены в `assets/characters/heroine.png` и `assets/characters/heroine-car.png`.
- Runtime-копии созданы в `public/assets`.
- Проверочный контактный лист сохранен в `assets/technical-preview/prepared-assets-contact-sheet.png`.
- Обновлены `docs/ASSET_LIST.md`, `docs/DESIGN.md`, `docs/PROJECT_STRUCTURE.md` и `docs/DECISIONS.md`.

### Next

- Перейти к screen mockup preview основного игрового экрана или запросить принятие Design-этапа.

### Stage 2. Design / Проектирование - уточнение шкалы выпадения бензина

- Статус: In Progress.
- Заказчик уточнил шкалу выпадения бензина.
- Зафиксирована первичная шкала выпадения бензина; актуальная runtime-шкала позднее уточнена.
- Обновлены `docs/REQUIREMENTS.md`, `docs/MACHINE_BONUS_SCENARIO.md`, `docs/ITEM_SPAWN_PROBABILITIES.md` и `docs/DECISIONS.md`.

### Next

- Продолжить Design-блок: HUD/effects или техническая подготовка ассетов перед Setup.

### Stage 2. Design / Проектирование - финализация desktop screen mockups

- Статус: Accepted.
- Заказчик утвердил финальные desktop-экраны для реализации.
- Финальный основной экран: `assets/technical-preview/screen-mockup-desktop-v27-hearts-more-left.png`.
- Финальный экран `Car Mode`: `assets/technical-preview/screen-mockup-car-mode-desktop-v7-scale-matched-wallet.png`.
- В `Car Mode` масштаб всего блока машины с героиней уменьшен так, чтобы ширина кошелька соответствовала основному desktop-экрану.
- Текущие preview-файлы обновлены: `assets/technical-preview/screen-mockup-desktop.png` и `assets/technical-preview/screen-mockup-car-mode-desktop.png`.
- Обновлены `docs/DESIGN.md` и `docs/DECISIONS.md`.

### Next

- Следующий рекомендуемый Design-preview: mobile screen mockup основного игрового экрана и `Car Mode`.

### Stage 2. Design / Проектирование - папка утвержденных визуалов

- Статус: Accepted.
- Создана папка `assets/approved/` для утвержденных изображений проекта.
- В `assets/approved/screens/desktop/` скопированы финальные desktop-экраны.
- В `assets/approved/characters/`, `assets/approved/concepts/`, `assets/approved/backgrounds/`, `assets/approved/items/` и `assets/approved/ui-icons/` скопированы утвержденные персонажи, концепты, фоны, игровые элементы и UI-значки/референсы.
- Добавлен `assets/approved/README.md` с правилом хранения: будущие явно согласованные изображения копируются в `assets/approved/` по категориям.
- Обновлены `docs/DESIGN.md`, `docs/PROJECT_STRUCTURE.md` и `docs/DECISIONS.md`.

### Next

- Продолжить Design-preview: mobile screen mockup основного игрового экрана и `Car Mode`.

### Stage 2. Design / Проектирование - обновление утвержденного mobile-фона

- Статус: Accepted.
- В `assets/approved/backgrounds/tomsk-mobile.png` скопирован загруженный файл `docs/references/visual/tomsk-collage-mobile.png`.
- Хэш источника и целевого файла совпадает; замена проверена визуально.
- Обновлен `docs/DECISIONS.md`.

### Next

- Использовать новый `assets/approved/backgrounds/tomsk-mobile.png` при подготовке mobile screen mockup основного экрана и `Car Mode`.

### Stage 2. Design / Проектирование - финализация mobile screen mockups

- Статус: Accepted.
- Заказчик принял mobile mockup с новыми размерами меню.
- Финальный основной mobile-экран: `assets/technical-preview/screen-mockup-mobile-v11-menu-final-widths.png`.
- Финальный mobile-экран `Car Mode`: `assets/technical-preview/screen-mockup-car-mode-mobile-v11-menu-final-widths.png`.
- Утвержденные копии сохранены в `assets/approved/screens/mobile/`.
- Зафиксирована mobile HUD-компоновка: левое меню 350 px, отступ слева 10 px; правое меню 335 px, отступ справа 10 px; расстояние между меню 15 px; высота обоих меню 102 px.
- Обновлены `docs/DESIGN.md`, `docs/SCREEN_FLOW.md`, `docs/DECISIONS.md` и `assets/approved/README.md`.

### Next

- Запросить принятие всего Design-этапа и подтверждение перехода к Stage 3. Environment Setup / Подготовка окружения.

### Stage Gate - Design принят

- Статус: Accepted.
- Заказчик принял весь этап `Design / Проектирование`.
- Заказчик разрешил перейти к `Stage 3. Environment Setup / Подготовка окружения`.
- Обновлен статус `docs/DESIGN.md`.
- Добавлено решение `DEC-047` в `docs/DECISIONS.md`.

### Stage 3. Environment Setup / Подготовка окружения - старт и аудит

- Статус: In Progress.
- Выполнена проверка текущего окружения без установки системных инструментов.
- `git`, `npm`, `npx`, `netlify`, `tsc` и `pnpm` не найдены в PATH.
- `node` в PATH найден, но запуск возвращает `Access denied`.
- Bundled Node Codex доступен: `v24.14.0`.
- Bundled `pnpm` Codex доступен через прямой запуск `pnpm.cjs`: `11.7.0`.
- Создан документ `docs/ENVIRONMENT_SETUP.md`.
- Обновлены `docs/ENVIRONMENT.md` и `docs/PROJECT_PLAN.md`.
- Обновлен `docs/ENVIRONMENT.docx`; файл пересобран из `docs/ENVIRONMENT.md` скриптом `scripts/build_environment_docx.py`.
- Структурная проверка `docs/ENVIRONMENT.docx` пройдена: документ открывается через `python-docx`, содержит 41 абзац и 4 таблицы.
- Визуальный рендер DOCX не выполнен: `soffice` / LibreOffice не найден в PATH.

### Next

- Согласовать способ подготовки каркаса проекта: временно через bundled Node/pnpm Codex или после установки Node.js LTS.

### Stage 3. Environment Setup / Подготовка окружения - Git for Windows

- Статус: Accepted.
- Заказчик установил Git for Windows.
- Проверена версия: `git version 2.55.0.windows.3`.
- Проверен Git LFS: `git-lfs/3.7.1`.
- Ветка по умолчанию для новых репозиториев настроена как `main`.
- Зафиксированы настройки Git: `core.autocrlf=true`, `credential.helper=manager`, `http.sslbackend=schannel`.
- В текущей Codex-сессии обычная команда `git` пока не видит обновленный PATH; прямой путь `C:\Program Files\Git\cmd\git.exe` работает. После перезапуска терминала команда `git` должна быть доступна через PATH.
- Обновлены `docs/ENVIRONMENT.md`, `docs/ENVIRONMENT.docx`, `docs/ENVIRONMENT_SETUP.md`, `docs/PROJECT_PLAN.md`, `docs/PROJECT_PLAN.docx` и `docs/DECISIONS.md`.
- Добавлен генератор `scripts/build_project_plan_docx.py` для воспроизводимого обновления Word-версии стартового плана.

### Next

- Перейти к установке Node.js LTS или отдельно подтвердить временное создание каркаса через bundled Node/pnpm Codex.

### Stage 3. Environment Setup / Подготовка окружения - Node.js LTS

- Статус: Accepted.
- Заказчик установил Node.js LTS.
- Проверена версия Node.js: `v24.18.0`.
- Проверена версия npm: `11.16.0`.
- Проверена версия npx: `11.16.0`.
- Заказчик настроил PowerShell Execution Policy `RemoteSigned`; в новой PowerShell-сессии `npm` и `npx` работают штатно.
- В текущей старой Codex-сессии `npm` и `npx` через `.ps1` еще могут блокироваться, поэтому для команд используется `npm.cmd` / `npx.cmd`.
- Обновлены `docs/ENVIRONMENT.md`, `docs/ENVIRONMENT_SETUP.md`, `docs/PROJECT_PLAN.md` и `docs/DECISIONS.md`.
- DOCX-файлы не обновлялись по новому правилу: обновлять `.docx` только по отдельной просьбе заказчика.

### Next

- Создать каркас проекта Vite + React + TypeScript и базовую структуру папок.

### Stage 3. Environment Setup / Подготовка окружения - Vite React TypeScript scaffold

- Статус: In Progress.
- Создан базовый проект Vite + React + TypeScript.
- Добавлены `package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `.gitignore`, `netlify.toml`, `README.md`.
- Созданы папки и базовые модули `src/app`, `src/game`, `src/ui`, `src/services`, `src/styles`, `netlify/functions`, `tests/game`.
- В `GameApp` создан первый технический игровой shell: фон, HUD, героиня, падающие предметы и переключатель `Car Mode`.
- Runtime-фоны в `public/assets/backgrounds` синхронизированы с утвержденными `assets/approved/backgrounds`.
- Установлены runtime-зависимости: `react`, `react-dom`, `lucide-react`, `@netlify/functions`, `@netlify/blobs`.
- Установлены dev-зависимости: `vite`, `typescript`, `@vitejs/plugin-react`, `vitest`, `@types/react`, `@types/react-dom`, `@types/node`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- Тесты: 2 файла, 6 проверок.
- Локальный Vite dev server запущен на `http://127.0.0.1:5173`, HTTP-проверка вернула `200`.
- `npm audit` показывает 6 moderate vulnerabilities в транзитивной цепочке `@netlify/blobs` / `@netlify/otel` / `@opentelemetry/*`; `npm audit fix --force` не выполнялся из-за риска breaking change.
- Автоматический screenshot через Playwright не выполнен: bundled Playwright в текущей среде не нашел `playwright-core`.
- Обновлены `docs/ENVIRONMENT_SETUP.md`, `docs/PROJECT_STRUCTURE.md`, `docs/PROJECT_PLAN.md`, `docs/DECISIONS.md` и `docs/PROJECT_LOG.md`.
- DOCX-файлы не обновлялись по правилу: только по отдельной просьбе заказчика.

### Next

- Визуально проверить локальный scaffold-экран в браузере: `http://127.0.0.1:5173`.
- После этого решить, закрываем ли Stage 3 или устанавливаем Netlify CLI до перехода к Development.

### Stage 3. Environment Setup / Подготовка окружения - принятие scaffold

- Статус: Accepted.
- Заказчик принял технический scaffold.
- Уточнено: текущий scaffold не является финальным переносом утвержденных mockups.
- Масштаб героини, масштаб машины, точные позиции HUD-кнопок и hitbox кошелька реализуются на этапе Development.
- Повторные проверки прошли: локальный сервер `http://127.0.0.1:5173` отвечает `200`, `npm.cmd run typecheck` passed, `npm.cmd run test` passed.
- Обновлены `docs/ENVIRONMENT_SETUP.md`, `docs/DECISIONS.md`, `docs/PROJECT_LOG.md`.
- DOCX-файлы не обновлялись по правилу: только по отдельной просьбе заказчика.

### Next

- Решить, ставим ли Netlify CLI сейчас или переносим ближе к Deployment.
- После этого запросить принятие всего Stage 3 и подтверждение перехода к Development.

### Stage Gate - Environment Setup принят

- Статус: Accepted.
- Заказчик принял весь `Stage 3. Environment Setup / Подготовка окружения`.
- Заказчик разрешил перейти к `Stage 4. Development / Разработка`.
- Netlify CLI переносится ближе к Deployment или к моменту активной проверки Netlify Functions.
- Обновлен статус `docs/ENVIRONMENT_SETUP.md`.
- Создан `docs/DEVELOPMENT.md`.
- Добавлено решение `DEC-052` в `docs/DECISIONS.md`.
- По просьбе заказчика обновлены `.docx`-версии документов: `docs/PROJECT_PLAN.docx`, `docs/ENVIRONMENT.docx`, `docs/ENVIRONMENT_SETUP.docx`.
- Структурная проверка `.docx` пройдена через `python-docx`.
- Визуальный рендер `.docx` не выполнен: `soffice` / LibreOffice не найден в PATH.

### Stage 4. Development / Разработка - старт

- Статус: In Progress.
- Первый рабочий блок: перенести утвержденные desktop/mobile mockups в рабочий React/Canvas экран.
- Особый фокус: точные HUD-позиции, масштаб героини и машины, кнопки пауза/рестарт/рекорды, responsive layout и подготовка hitbox кошелька.

### Next

- Начать Development-блок: основной игровой экран по утвержденным mockups.

### Stage 4. Development / Разработка - блок 1: основной игровой экран

- Статус: In Review.
- Перенесены утвержденные desktop/mobile HUD-размеры в рабочий React/CSS экран.
- Кнопки `Пауза`, `Рестарт`, `Рекорды` размещены в левом HUD рядом с жизнями.
- Правый HUD показывает сборку машины, статус и бензин.
- Обычный режим и `Car Mode` используют утвержденные character assets.
- Добавлена техническая зона кошелька и модуль `src/game/data/layout.ts` для будущей hitbox-логики.
- Добавлен тест `tests/game/layout.test.ts`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- Локальный dev server `http://127.0.0.1:5173` отвечает `200`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Next

- Заказчик проверяет основной экран локально и принимает или возвращает правки по Development-блоку 1.
- После принятия перейти к следующему Development-блоку: игровой цикл, движение героини и первичная логика падения/ловли предметов.

### Stage 4. Development / Разработка - блок 1: Review-правка 2

- Статус: Accepted.
- Desktop левый HUD изменен на `365 x 104 px`, отступ слева сохранен.
- Desktop правый HUD изменен на `365 x 104 px`, отступ справа сохранен.
- Группа кнопок в левом HUD сдвинута влево на `25 px`.
- Блок канистры и бензина в правом HUD сдвинут влево на `25 px`.
- На desktop-основном экране скрыта надпись `Собери машину`; mobile-верстка сохраняет ее.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.

### Stage 4. Development / Разработка - блок 1 принят

- Статус: Accepted.
- Заказчик принял корректировки основного desktop-экрана и `Car Mode` preview.
- Development-блок 1 считается закрытым.

### Next

- Перейти к Development-блоку 2: игровой цикл, движение героини, падение предметов, первичная логика ловли кошельком и пропусков.

### Stage 4. Development / Разработка - блок 2: игровой цикл

- Статус: In Review.
- Реализован игровой цикл через `requestAnimationFrame`.
- Реализовано движение героини по горизонтали: клавиши `ArrowLeft` / `ArrowRight`, `A` / `D`, а также touch/drag управление.
- Реализовано появление и падение предметов сверху вниз.
- Реализована ловля через hitbox кошелька.
- Реализованы очки, штрафы, жизни, сбор деталей машины, бензин и включение `Car Mode`.
- Реализовано убывание бензина в `Car Mode` по правилу `1 л = 1 секунда`.
- Реализована механика пропусков: налог/штраф без наказания, договор `-1 жизнь`, остальные ловимые предметы дают счетчик пропусков `0/3`, на третьем пропуске `-1 жизнь`.
- Счетчик пропусков отображается слева под HUD.
- Добавлены тесты `tests/game/gameplay.test.ts`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Next

- Заказчик проверяет playable loop локально и принимает или возвращает правки по Development-блоку 2.

### Stage 4. Development / Разработка - блок 2: Review-правка 5

- Статус: In Review.
- Заказчик принял предложенный баланс: героиня без авто `360 px/s`, машина `520 px/s`, базовое падение `110 px/s`, рост падения `100%-165%`, интервал появления `1200-620 мс`.
- Реализованы fair spawn rules: полезные предметы не появляются одновременно на противоположных краях экрана.
- Договор, кухня и корпус машины не дублируются, пока такой же предмет уже активен на экране.
- Ограничение договора `8 секунд` теперь считается от момента появления договора.
- Исправлена вероятная причина раннего `Game Over`: после потери жизни действует защита `700 мс` от повторного мгновенного списания жизни; при `gameOver` обработка предметов в текущем кадре останавливается.
- Добавлены тесты `tests/game/difficulty.test.ts` и `tests/game/spawning.test.ts`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 6

- Статус: In Review.
- Купюры исключены из счетчика пропусков: они дают очки при ловле, но не наказывают при падении мимо.
- Счетчик пропусков теперь увеличивают мебель, детали машины и канистра бензина.
- Клавиша `Пробел` запускает игру из начального/конечного состояния, ставит активную игру на паузу и продолжает игру из паузы.
- Высота обычной героини синхронизирована с высотой `Car Mode`; размеры прямоугольника кошелька выровнены.
- Границы движения теперь считаются по прямоугольнику кошелька, поэтому героиня/машина могут частично уходить за экран до границы кошелька.
- Добавлены тесты на пропуски купюр и равенство высот sprite.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 7

- Статус: Accepted.
- Пропорция вредных предметов изменена с `65% Налог / 35% Штраф` на `70% Налог / 30% Штраф`.
- Общая доля вредных предметов по уровням сложности остается прежней: `6%-15%`.
- Добавлен тест на выбор вредного предмета по новой пропорции.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 8

- Статус: Accepted.
- Шкала выпадения бензина была изменена:
  - машина не собрана, бензина меньше 60 л: `4%`;
  - машина не собрана, бензина 60 л: `0%`;
  - машина собрана, бензина 0 л: `6%`;
  - машина активна, бензина 1-20 л: `5%`;
  - машина активна, бензина 21-40 л: `4%`;
  - машина активна, бензина 41-60 л: `2%`.
- Актуальная runtime-шкала позднее уточнена в Review-правке 10.
- Обновлен тест `tests/game/machineBonus.test.ts`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 9

- Статус: In Review.
- В HUD машины статус `Нужен бензин` сдвинут на `8 px` влево от базовой позиции.
- Размеры правого HUD, канистра, счетчик бензина и слоты деталей не изменялись.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 10

- Статус: Accepted.
- Шкала выпадения бензина уточнена:
  - машина не собрана, бензина меньше 60 л: `4%`;
  - машина не собрана, бензина 60 л: `0%`;
  - машина собрана, бензина 0 л: `6%`;
  - `Car Mode`, бензина 1-10 л: `8%` (позднее уточнено до `10%` в Review-правке 12);
  - `Car Mode`, бензина 11-20 л: `6%`;
  - `Car Mode`, бензина 21-40 л: `4%`;
  - `Car Mode`, бензина 41-60 л: `3%`.
- Актуальный верхний диапазон `Car Mode 41-60 л` позднее уточнен до `2%`.
- Добавлено правило `Low Fuel Assist`: если в `Car Mode` бензина 10 л или меньше, шанс канистры был `8%`; актуальное значение позднее уточнено до `10%` в Review-правке 12.
- Канистра бензина падает на `15%` медленнее обычных легких предметов.
- `Pity Timer` не был включен в этой версии; актуальное правило позднее добавлено в Review-правке 13.
- Обновлены тесты `tests/game/machineBonus.test.ts` и `tests/game/spawning.test.ts`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 11

- Статус: Accepted.
- Множитель скорости падения на уровне 10 изменен с `165%` на `180%`; актуальный runtime-максимум позднее уточнен в Review-правках 12 и 14.
- Шанс канистры в `Car Mode` при бензине 41-60 л изменен с `3%` на `2%`.
- Остальные правила бензина не изменялись.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 12

- Статус: Accepted.
- Уровень сложности был в диапазоне `1-10`; актуальный диапазон позднее уточнен в Review-правке 14.
- Рост сложности по времени отключен.
- Рост сложности по очкам был изменен на `+1 уровень за каждые 300000 очков`; актуальный шаг позднее уточнен в Review-правке 14.
- Множитель скорости падения на уровне 10 изменен с `180%` на `190%`; актуальный runtime-максимум позднее уточнен в Review-правке 14.
- Шанс канистры в `Car Mode` при бензине 1-10 л изменен с `8%` на `10%`.
- Остальные правила бензина не изменялись.
- Обновлены тесты `tests/game/difficulty.test.ts` и `tests/game/machineBonus.test.ts`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 13

- Статус: Accepted.
- Добавлен `Pity Timer` для бензина.
- Если в `Car Mode` бензина 20 л или меньше и за последние 12 секунд не появлялась канистра, следующая подходящая генерация предмета принудительно дает канистру.
- Если канистра уже активна на экране, принудительная генерация не создает вторую канистру.
- Таймер считается от момента появления канистры.
- Обновлены тесты `tests/game/spawning.test.ts`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Review-правка 14

- Статус: Accepted.
- Уровень сложности изменен на диапазон `1-20`.
- Рост сложности по времени остается отключенным.
- Рост сложности по очкам изменен на `+1 уровень за каждые 200000 очков`.
- Множитель скорости падения на уровне 20 изменен на `400%`.
- Интервал появления предметов масштабируется от `1200 мс` на уровне 1 до `620 мс` на уровне 20.
- Доля вредных предметов и доля договора масштабируются по новой шкале `1-20`, сохраняя прежние границы `6%-15%` и `2%-5%`.
- Обновлены тесты `tests/game/difficulty.test.ts`.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test`, `npm.cmd run build`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 2: Stage Gate

- Статус: Accepted.
- Заказчик принял Development-блок 2: игровой цикл, движение героини, падение предметов, ловля кошельком и правила пропусков.
- Заказчик разрешил переход к Development-блоку 3: `Game Over`, имя игрока, `playerId` и таблица рекордов.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Game Over, имя игрока, playerId и таблица рекордов

- Статус: In Review.
- Реализован `Game Over` с итоговым счетом, вводом имени, сохранением результата, top-10 и повторным запуском игры.
- Реализован анонимный локальный `playerId` через `localStorage`.
- Реализована таблица рекордов через Netlify Function `netlify/functions/scores.ts` по согласованному API-контракту.
- Реализован frontend fallback через `localStorage`: при недоступности API результат сохраняется локально и как pending-result.
- Для одного `playerId` сохраняется только лучший результат; более низкий результат может обновить отображаемое имя, но не заменяет счет.
- Таблица рекордов показывает top-10 и подсвечивает текущего игрока.
- Backend API не возвращает raw `playerId` в публичном списке; текущий игрок отмечается флагом `isCurrentPlayer`.
- Добавлены тесты для логики рекордов, локального хранения, frontend API-сервиса и Netlify Function.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test` - 10 files / 41 tests, `npm.cmd run build`.
- Runtime-проверка пройдена: `http://127.0.0.1:5173/` и `http://127.0.0.1:5173/?mode=car` отвечают `200`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 1

- Статус: In Review.
- Заказчик выявил, что новое имя на том же ноутбуке заменяло имя существующего рекорда.
- Причина: текущая реализация использовала один `playerId` на браузер.
- Реализация изменена на несколько локальных профилей на одном устройстве: каждое новое имя получает отдельный `playerId`, существующее имя использует прежний `playerId`.
- Backend API и Netlify Blobs не требуют новой базы данных или нового окружения.
- Обновлены `src/services/playerIdentity.ts`, `src/app/GameApp.tsx`, тесты и markdown-документы.
- Проверки пройдены: `npm.cmd run typecheck`, `npm.cmd run test` - 10 files / 44 tests, `npm.cmd run build`.
- Runtime-проверка пройдена: `http://127.0.0.1:5173/` и `http://127.0.0.1:5173/?mode=car` отвечают `200`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - asset correction: contract

- Статус: Applied.
- Принятый ассет `contract` заменен в `assets/approved/items/contract.png` и `public/assets/items/contract.png`.
- Добавлена зеленая рамка по периметру в стиле `fine`, цветовая логика взята от денежных купюр.
- Исправлена прозрачность фона: оба файла сохранены как `Format32bppArgb`, углы имеют alpha `0`.
- DOCX не обновлялись: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Stage Gate и title screen

- Статус: Accepted.
- Заказчик принял цепочку `Game Over -> имя -> локальный playerId -> сохранение результата -> отображение в top-10`.
- Утвержденный прозрачный title asset из `game-title-reference.jpeg` сохранен в `assets/approved/ui/game-title.png` и runtime-копию `public/assets/ui/game-title.png`.
- Название игры добавлено на стартовый экран и экран паузы.
- Desktop-позиция соответствует последнему принятому preview: ширина `539px`, верхний отступ `64px`, центрирование по горизонтали между верхними HUD.
- Для mobile добавлена адаптация: title размещается ниже верхних HUD, по центру, чтобы не пересекаться с меню.
- Проверки пройдены: `npm.cmd run test` - 10 files / 44 tests, `npm.cmd run build`.
- Runtime-проверка пройдена: `http://127.0.0.1:5173/` отвечает `200`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage Gate: Stage 4 accepted, Stage 5 Testing started

Дата: 2026-07-27

- Статус Stage 4. Development / Разработка: Accepted.
- Заказчик подтвердил, что эффект визуальных артефактов пойманных предметов прошел.
- Финальная проверка Development-блока 4 выполнена заказчиком по списку:
  - основной экран и Car Mode в production preview;
  - старт, пауза, рестарт, рекорды, Game Over;
  - ловля, пропуски, штрафы, договор, бензин, сборка машины;
  - high-level режим с `score=4000000` или выше.
- Последняя исправленная проблема: при ловле нескольких предметов одновременно на высокой скорости были видны артефакты чуть выше зоны ловли над кошельком; исправлено немедленным скрытием удаленных DOM-элементов пойманных предметов до React-unmount.
- Проверка после исправления: `npm.cmd run build` - passed.
- Свежий production preview:
  - основной экран: `http://127.0.0.1:4175/`;
  - Car Mode high-level: `http://127.0.0.1:4175/?mode=car&score=4000000&fuel=60`.
- Stage Gate: заказчик разрешил переход к `Stage 5. Testing / Тестирование`.
- Статус Stage 5. Testing / Тестирование: In Progress.
- Основной артефакт Stage 5: `docs/TEST_CHECKLIST.md`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### DOCX update after Stage 5 transition

Дата: 2026-07-27

- По отдельной просьбе заказчика обновлены существующие `.docx`-версии документов:
  - `docs/PROJECT_PLAN.docx`;
  - `docs/ENVIRONMENT.docx`;
  - `docs/ENVIRONMENT_SETUP.docx`.
- `docs/PROJECT_PLAN.docx` пересобран из актуального `docs/PROJECT_PLAN.md` и содержит текущий статус `Stage 5. Testing / Тестирование`, принятие Stage 4 и артефакт `docs/TEST_CHECKLIST.md`.
- Структурная проверка `.docx` пройдена через `python-docx`: документы открываются, абзацы и таблицы читаются.
- Визуальный render QA через LibreOffice не выполнен: в текущем окружении не найден `soffice` / LibreOffice.

### Stage 5. Testing / Тестирование - прогон 1

Дата: 2026-07-27

- Статус: In Progress.
- Выполнены технические проверки:
  - `npm.cmd run typecheck` - passed;
  - `npm.cmd run test` - passed, 10 files / 48 tests;
  - `npm.cmd run build` - passed.
- Поднят свежий production preview после сборки: `http://127.0.0.1:4179/`.
- High-level Car Mode URL для проверки: `http://127.0.0.1:4179/?mode=car&score=4000000&fuel=60`.
- Выполнен visual smoke через системный Edge:
  - desktop main;
  - desktop Car Mode high-level;
  - mobile main `390 x 844`.
- Выполнен interactive smoke:
  - main: click Start, Pause, Records, Close, Restart No, Restart Yes;
  - Car Mode: Pause, Records, Restart No сохраняют heroine-car, Restart Yes сбрасывает в стартовый экран.
- Найден и исправлен дефект `T5-001`: на mobile viewport `390 x 844` верхние HUD-панели частично пересекались; добавлены compact-mobile CSS-правила для сердечек, HUD-кнопок и иконок.
- Найден и исправлен дефект `T5-002`: click по кнопке `Старт` не запускал игру из-за перехвата pointer-событий игровым полем; для кнопки `Старт` добавлена остановка pointer bubbling.
- Тестовые скриншоты сохранены в `assets/source-previews/testing/`.
- Остается проверить вручную в Stage 5:
  - 3-5 минут gameplay/playtest ловли и пропусков;
  - сборку машины и расход бензина в реальной игре;
  - Game Over -> ввод имени -> playerId -> top-10;
  - API/fallback поведение перед Deployment.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 5. Testing / Тестирование - ручной playtest

Дата: 2026-07-27

- Статус: Passed for gameplay playtest.
- Заказчик сообщил, что ручной 3-5 минутный playtest пройден положительно.
- По результату ручной проверки отмечены как пройденные:
  - gameplay: очки, жизни, пропуски, договор, налог, штраф;
  - machine bonus: сборка машины, бензин, переход в Car Mode и расход топлива;
  - smoothness: плавность героини, Car Mode и падения предметов в production preview;
  - regression по последним UX-правкам.
- Остаются отдельными проверками Stage 5:
  - Game Over -> ввод имени -> локальный playerId -> top-10;
  - API/fallback поведение перед Deployment.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 2

- Статус: Applied.
- Заказчик принял новую более плавную и длинную шкалу сложности.
- Уровни сложности изменены на `1-40`.
- Рост сложности по времени остается отключенным.
- Рост сложности по очкам: `+1 уровень за каждые 200000 очков`.
- Темп уровня 40 принят как `400%` скорости падения и `650 мс` интервала появления предметов, примерно `92` предмета в минуту.
- Базовая скорость падения изменена на `100 px/s`.
- Доля вредных предметов масштабируется от `6%` до `22%`, пропорция `Налог 70% / Штраф 30%` сохранена.
- Бензин: до сборки машины `3%`; `Car Mode` 1-10 л `8%`, 11-20 л `6%`, 21-40 л `4%`, 41-60 л `2%`; `Pity Timer` - `15 секунд`.
- Детали автомобиля сделаны редкими: `4-7%` по диапазонам уровней и минимальная задержка `8 секунд` между деталями.
- Обновлены код, тесты и markdown-документы баланса.
- Проверки пройдены: `npm.cmd run test` - 10 files / 47 tests, `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 3

- Статус: Applied.
- Добавлено правило динамики: каждый новый уровень сложности увеличивает скорость движения героини без авто и в `Car Mode` на `1%`.
- Уровень 1: героиня `360 px/s`, `Car Mode` `520 px/s`.
- Уровень 40: героиня примерно `500 px/s`, `Car Mode` примерно `723 px/s`.
- Обновлены код, тесты и markdown-документы баланса.
- Проверки пройдены: `npm.cmd run test` - 10 files / 48 tests, `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 4

- Статус: Applied.
- Активная зона кошелька, показанная прямоугольником и используемая для столкновений, уменьшена на `5%`.
- Desktop: было `168 x 118 px`, стало `159.6 x 112.1 px`.
- Mobile: было `180 x 145 px`, стало `171 x 137.75 px`.
- Центр зоны ловли не смещался.
- Проверка пройдена: `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 9

- Статус: Applied.
- Изменен базовый баланс горизонтального движения.
- Героиня без авто: `220 px/s`.
- Героиня в авто / `Car Mode`: `340 px/s`.
- Динамический рост изменен на диапазонный: уровни `1-20` добавляют `+1%` за уровень, уровни `21-40` добавляют `+2%` за уровень от базовой скорости.
- На уровне 40 применяется множитель `159%`: героиня без авто примерно `350 px/s`, `Car Mode` примерно `541 px/s`.
- Проверки пройдены: `npm.cmd run test` - 10 files / 48 tests, `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 7

- Статус: Applied.
- Активная зона кошелька на desktop уменьшена с `150 x 100 px` до `145 x 100 px`.
- Уменьшение выполнено как подрезка `5 px` с правой стороны: левая граница оставлена на прежнем месте.
- Mobile-зона не изменялась: `170 x 135 px`.
- Проверка пройдена: `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 6

- Статус: Applied.
- Активная зона кошелька на desktop сдвинута на `5 px` вправо.
- Размер desktop-зоны не изменялся: `150 x 100 px`.
- Mobile-положение оставлено без изменения.
- Проверка пройдена: `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 5

- Статус: Applied.
- Активная зона кошелька уменьшена до заданных размеров без смещения центра.
- Desktop: `150 x 100 px`.
- Mobile: `170 x 135 px`.
- Проверка пройдена: `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 3: Review-правка 8

- Статус: Applied.
- Активная зона кошелька на desktop изменена до `150 x 105 px`.
- Увеличение высоты выполнено на `5 px` вверх: нижняя граница зоны оставлена на прежнем месте.
- Mobile-зона не изменялась: `170 x 135 px`.
- Проверка пройдена: `npm.cmd run build`.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 4. Development / Разработка - блок 4: Полировка игрового процесса и UX-состояний

- Статус: In Review.
- Проверен баланс после последних правок скорости движения и шкалы падения.
- Исправлено подтверждение `Restart` в активной игре и `Car Mode`: отмена возвращает игрока в исходный активный режим, подтверждение сбрасывает попытку.
- `Records` теперь можно открыть из паузы; закрытие возвращает игру в состояние паузы.
- Улучшена читаемость mobile HUD для статуса машины.
- Проверочные скриншоты сохранены в `assets/source-previews/polish/`.
- Проверки пройдены: `npm.cmd run test` - 10 files / 48 tests, `npm.cmd run build`.
- Перед Testing остается ручная проверка кликов и 3-5 минутный playtest desktop/mobile.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 5. Testing / Тестирование - рекорды и хранение данных

Дата: 2026-07-27

- Статус: Passed for leaderboard/storage chain.
- Выполнена отдельная проверка цепочки `Game Over -> ввод имени -> локальный playerId -> сохранение результата -> отображение в top-10`.
- Targeted tests пройдены: `npm.cmd run test -- tests/game/leaderboard.test.ts tests/services/localLeaderboard.test.ts tests/services/leaderboardApi.test.ts tests/netlify/scoresFunction.test.ts` - 4 files / 18 tests.
- Live UI проверка выполнена в production preview через системный Edge:
  - URL: `http://127.0.0.1:4179/?mode=car&score=7800000&fuel=60`;
  - Game Over достигнут за 62 секунды;
  - результат сохранен под тестовым именем `QA Alpha`;
  - top-10 на экране содержит сохраненное имя.
- Проверены browser storage keys:
  - `cod-web-game:playerProfiles`;
  - `cod-web-game:playerName`;
  - `cod-web-game:localScores`;
  - `cod-web-game:pendingScore`.
- Поведение нескольких имен на одном устройстве подтверждено тестами:
  - новое имя получает отдельный локальный `playerId`;
  - повторное использование того же имени переиспользует прежний профиль;
  - для одного `playerId` сохраняется только лучший счет.
- Локальный fallback подтвержден: при недоступном локальном API результат сохраняется в `localScores` и как `pendingScore` для последующей отправки.
- Live-проверка Netlify API остается отдельной задачей Stage 6. Deployment / Размещение.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 5. Testing / Тестирование - скрытие debug-hitbox кошелька

Дата: 2026-07-27

- Статус: Applied.
- Активная зона кошелька была нужна для настройки ловли предметов, но в пользовательской версии игры должна быть скрыта.
- В `.wallet-target` убраны видимые border/background/shadow и задано `opacity: 0`.
- DOM-элемент сохранен, чтобы runtime-логика продолжала измерять hitbox через `getBoundingClientRect()`.
- Браузерная проверка подтвердила: элемент остается `display: block`, имеет размеры и не виден игроку.
- Добавлен dev-переключатель `debugHitbox=1`: при наличии query-параметра игровое поле получает класс `show-wallet-hitbox`, и зона кошелька временно показывается для визуальной настройки.
- Debug-рамка сделана через `box-shadow`, а не через `border`, чтобы видимое отображение не меняло реальные размеры hitbox.
- Проверка подтвердила:
  - обычный режим: `opacity: 0`, размеры `136 x 95 px` на desktop preview;
  - debug-режим: `opacity: 1`, размеры `136 x 95 px` на desktop preview.
- Проверки пройдены:
  - `npm.cmd run test` - 10 files / 48 tests;
  - `npm.cmd run build` - passed.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 5. Testing / Тестирование - правило ловли по центру предмета

Дата: 2026-07-27

- Статус: Rolled back.
- Форма активной зоны кошелька остается прямоугольной.
- Временная правка была проверена в playtest и отменена по решению заказчика: предыдущая модель ловли ощущается лучше.
- Активное правило снова прежнее:
  - у предмета есть прямоугольник;
  - у кошелька есть прямоугольник;
  - предмет считается пойманным, если эти два прямоугольника пересеклись.
- Rollback:
  - удалена функция `rectCenterInside()` из `src/game/engine/collision.ts`;
  - игровой цикл в `src/app/GameApp.tsx` снова использует `intersects(itemRect, walletRect)`;
  - удален временный тест `tests/game/collision.test.ts`.
- Проверки после rollback пройдены:
  - `npm.cmd run test` - 10 files / 48 tests;
  - `npm.cmd run build` - passed.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 5. Testing / Тестирование - ссылка с иконки авто в окне рестарта

Дата: 2026-07-27

- Статус: Applied.
- В модальном окне `Рестарт?` кликабельная иконка героини в авто теперь ведет на `https://tomsk-mebel70.ru/`.
- Реализация: `button.restart-confirm-car-button` заменен на `a.restart-confirm-car-button` с `target="_blank"` и `rel="noopener noreferrer"`.
- Существующие hover/click-эффекты и comic tooltip сохранены.
- Дополнительно сохранено ощущение кнопки после открытия ссылки: для `a.restart-confirm-car-button` задан `cursor: pointer`, убрано стандартное подчеркивание ссылки, добавлены стабильные hover/focus/active transform-состояния.
- После клика по ссылке focus сбрасывается через `onPointerUp` и `onClick`, чтобы при возврате в игру повторное наведение снова запускало визуальное увеличение.
- UI-проверка подтвердила:
  - tag: `A`;
  - href: `https://tomsk-mebel70.ru/`;
  - target: `_blank`;
  - rel: `noopener noreferrer`.
  - link UI: `cursor: pointer`, `text-decoration: none`, transition по `transform`;
  - после клика focus сброшен, повторный hover снова дает `scale(1.02)`.
- Проверки пройдены:
  - `npm.cmd run test` - 10 files / 48 tests;
  - `npm.cmd run build` - passed.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 5. Testing / Тестирование - закрывающая проверка

Дата: 2026-07-27

- Статус: Ready for Acceptance.
- Выполнена закрывающая техническая проверка:
  - `npm.cmd run typecheck` - passed;
  - `npm.cmd run test` - passed, 10 files / 48 tests;
  - `npm.cmd run build` - passed.
- Выполнен закрывающий production smoke через системный Edge на `http://127.0.0.1:4179/`.
- Проверены основные UX-сценарии:
  - старт;
  - пауза;
  - рестарт;
  - рекорды;
  - ссылка с иконки героини в авто на `https://tomsk-mebel70.ru/`;
  - сохранение hover/click-эффекта иконки авто после открытия ссылки;
  - скрытый hitbox кошелька в обычном режиме;
  - видимый hitbox при `debugHitbox=1`;
  - `Car Mode` сохраняется после паузы, открытия/закрытия рекордов и отмены рестарта.
- Проверка подтвердила, что критических дефектов перед Stage Gate не осталось.
- Stage 5 готов к приемке заказчиком. Переход к Stage 6. Deployment / Размещение возможен только после явного принятия Stage 5.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage Gate: Stage 5 accepted, Stage 6 Deployment started

Дата: 2026-07-27

- Статус Stage 5. Testing / Тестирование: Accepted.
- Заказчик принял Stage 5. Testing и разрешил переход к Stage 6. Deployment / Размещение.
- Текущий этап проекта: `Stage 6. Deployment / Размещение`.
- Созданы стартовые артефакты Stage 6:
  - `docs/DEPLOYMENT.md`;
  - `docs/CHANGELOG.md`.
- Обновлены:
  - `docs/PROJECT_PLAN.md`;
  - `docs/TEST_CHECKLIST.md`;
  - `docs/PROJECT_LOG.md`;
  - `docs/DECISIONS.md`.
- Первый фокус Stage 6: подготовка Git/GitHub/Netlify, проверка Netlify CLI, deploy configuration и production-проверка таблицы рекордов.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.

### Stage 6. Deployment / Размещение - GitHub repository

Дата: 2026-07-27

- Статус: Applied.
- GitHub-репозиторий создан: `https://github.com/bihregwork/cod-web-game.git`.
- Локальный Git-репозиторий инициализирован в `C:\Users\Andrew\Documents\Cod_Web_Game`.
- Основная ветка: `main`.
- Remote `origin` подключен к `https://github.com/bihregwork/cod-web-game.git`.
- `.gitignore` дополнен правилами для логов, Python cache и временных Word lock-файлов.
- Из staging исключены:
  - `docs/~$OJECT_PLAN.docx`;
  - `scripts/__pycache__/`.
- Первый commit создан: `9283ab3 Initial game release`.
- Push выполнен успешно: `main -> origin/main`.
- Рабочее дерево после push чистое.
- Следующий шаг Stage 6: подключить GitHub-репозиторий к Netlify и проверить build/deploy settings.
- DOCX не обновлялся: обновление `.docx` выполняется только по отдельной просьбе заказчика.
