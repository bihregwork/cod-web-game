# Cod Web Game

Легкая веб-игра на Vite + React + TypeScript.

## Команды

```powershell
npm install
npm run dev
npm run build
npm run test
```

В старой Codex-сессии PowerShell можно использовать `npm.cmd` вместо `npm`.
